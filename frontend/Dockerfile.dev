# Development Dockerfile for Frontend
# Uses dev server instead of production build

FROM node:22-slim

WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install

# Copy project (will be overridden by volume mount in docker-compose.override.yml)
COPY . .

EXPOSE 5173

# Run dev server with hot-reloading
CMD ["npm", "run", "dev", "--", "--host"]
